{
  "name": "ssaucsd",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "check": "bun run lint && bun run typecheck && bun run format:check",
    "format": "bunx prettier --write .",
    "format:check": "turbo format:check",
    "convex:dev": "bunx convex dev",
    "convex:deploy": "bunx convex deploy",
    "data:migrate": "bun run scripts/migrate-supabase-to-convex.ts"
  },
  "devDependencies": {
    "pg": "^8.18.0",
    "prettier": "^3.7.4",
    "turbo": "^2.8.12",
    "typescript": "^5.9.3"
  },
  "packageManager": "bun@1.3.6",
  "dependencies": {
    "convex": "^1.32.0"
  }
}
